%ul.list-inline.pt-10.mb-25.mr-240
  %li.mr-35
    %a.filter-link{href: "#"}
      = t('all')
      %span.badge.general-badge
        = categories.map(&:books).sum(&:size)
  = render partial: 'partials/categories_line_item', collection: categories