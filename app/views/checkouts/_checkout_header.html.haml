%h1.general-title-margin
  = t('.checkout')
%ul.steps.list-inline
  = render partial: 'step', collection: Order.aasm.states.map(&:name), spacer_template: 'step_line', locals: { state_index: Order.aasm.states.map(&:name).find_index(state.to_sym) }
